{
  "name": "svent",
  "version": "0.0.8",
  "description": "Svent is an async event framework implemented with Fiber.Used for game or GUI event handling.",
  "main": "lib/svent.js",
  "keywords": [
    "svent",
    "async",
    "event"
  ],
  "scripts": {
    "clean": "rm -rf lib",
    "build": "npm run clean &&babel src -d lib",
    "test": "npm run build && node test/runner.js",
    "docs": "jsdoc src/svent/ src -d doc"
  },
  "author": "shitake",
  "license": "MIT",
  "dependencies": {
    "fibers": "1.0.15"
  },
  "devDependencies": {
    "babel-cli": "^6.0.0",
    "babel-preset-es2015": "^6.24.1",
    "colors": "1.1.2"
  }
}
